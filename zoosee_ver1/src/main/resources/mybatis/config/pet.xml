<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pet">

	<insert id="registerPet" parameterType="petVO">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			select pet_seq.nextval from dual
		</selectKey>
		insert into pet(petNo,petName,petAge,petType,petGender,petNeutral,pet_img,pet_size,clob,id)
		values(#{petNo},#{petName},#{petAge},#{petType},#{petGender},#{petNeutral},#{petImg},#{petSize},#{clob},#{memberVO.id})
	</insert>
	
	<!-- <resultMap type="petVO" id="petRM">
		<result property="memberVO.id" column="id" />	
	</resultMap> -->
	
</mapper>	